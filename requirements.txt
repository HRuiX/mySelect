# HCDS 依赖项
# Hierarchical Clustering + Dynamic Sampling

# ==================== 核心依赖 ====================
numpy>=1.21.0
pydantic>=2.0.0
pyyaml>=6.0
tqdm>=4.60.0
psutil>=5.8.0

# ==================== 嵌入计算 ====================
sentence-transformers>=2.2.0
torch>=2.0.0
transformers>=4.30.0

# ==================== 聚类 ====================
hdbscan>=0.8.29
scikit-learn>=1.0.0

# ==================== GPU 加速聚类 (cuML) ====================
# cuML 提供 GPU 加速的 HDBSCAN，需要 CUDA 环境
# 安装方式: conda install -c rapidsai -c nvidia cuml cudf
# 或通过 pip: pip install cuml-cu12 (根据 CUDA 版本选择)
# cuml-cu12>=24.0.0  # 取消注释以启用 GPU HDBSCAN
# cupy-cuda12x>=12.0.0  # cuML 依赖

# ==================== 性能优化 ====================
numba>=0.56.0  # JIT 加速 FPS 算法

# ==================== 向量检索 (大规模) ====================
faiss-gpu>=1.7.0

# ==================== 数据存储 ====================
h5py>=3.7.0
pyarrow>=10.0.0  # Parquet 支持

# ==================== 可视化 (可选) ====================
matplotlib>=3.5.0

# ==================== HuggingFace 数据集 (可选) ====================
datasets>=2.10.0

# ==================== OpenAI API (可选) ====================
openai>=1.0.0

# ==================== 分布式 (可选) ====================
# ray>=2.0.0

# ==================== 开发依赖 ====================
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# isort>=5.10.0
# mypy>=1.0.0
